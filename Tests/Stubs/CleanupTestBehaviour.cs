using UnityEngine;

namespace Yozolab.DaerD.Tests
{
    /// <summary>Stands in for a VRC behaviour (the SDK isn't referenced by the tests).</summary>
    public class CleanupTestBehaviour : StateMachineBehaviour
    {
    }
}
